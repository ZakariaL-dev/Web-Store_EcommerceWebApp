// My Components
import AccountProfileInfos from "@/components/AccountComponents/AccountProfileInfos";

export const metadata = {
  title: "Profile Infos",
};

const page = () => {
  return <AccountProfileInfos />;
};

export default page;
