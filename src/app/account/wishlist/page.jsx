// My Components
import WishListWrapper from "@/components/AccountComponents/PageWrappers/WishListWrapper";

export const metadata = {
  title: "Wishlist",
};

const page = () => {
  return <WishListWrapper />;
};

export default page;
